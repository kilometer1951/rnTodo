import merge from 'webpack-merge';
import path from 'path';
import WebpackDevAppConfiguration from './webpack.app.dev.babel';

export default merge(
    WebpackDevAppConfiguration,
    {
        mode: 'production'
    }
);
